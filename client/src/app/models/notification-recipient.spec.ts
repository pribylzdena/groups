import { NotificationRecipient } from './notification-recipient';

describe('NotificationRecipient', () => {
  it('should create an instance', () => {
    expect(new NotificationRecipient()).toBeTruthy();
  });
});
